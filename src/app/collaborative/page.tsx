import { CollaborativeWorkspace } from "@/components/planner/CollaborativeWorkspace";

export default function CollaborativePage() {
    return <CollaborativeWorkspace />;
}

export const metadata = {
    title: "Collaborative Travel Workspace | AI Agent",
    description: "Plan your next trip with our autonomous travel agent on an interactive map.",
};
